spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
        show-sql: true
        ddl-auto: none
  datasource:
    url: jdbc:postgresql://localhost:5432/person_db
    username: postgres
    password: ${POSTGRES_PASSWORD:postgres123456}